# 打鍵visualizerを作る

指定された期間に入力されたイベントを記録しておき、後からその記録を元に操作履歴を視覚化した動画を生成する

## 機能1: キーロガー機能

動画生成に必要な情報を時刻とともに記録する

### 要求仕様

- IME本体に組み込み
- ロガーモードのスタートとストップをメニューから指示し、その間のキー入力をミリ秒解像度程度の時刻とともに記録する
- 基本キーだけではなく、部首変換、交ぜ書き変換結果で入力した文字、Backspaceを送信したイベントも記録する
- モード切り換えイベント、変換せずアプリケーションにそのまま送信したイベントも記録する
- IMEプロセスが再起動されても、ロガーモードのon/offが保たれるように記録する

### 設計方針案

- ロガーモードの記録はconfigディレクトリにファイルを作成する
- ロガーのイベント列は、configディレクトリにファイルを作成して記録する
    - 追記しやすいようにjsonlがよさそう
    - ファイル名にスタート時刻をつけるとよさそう
        - 別案: 固定のファイル名で、スタートするときにクリアして上書きする
- ログに記録する情報
    - イベント発生のタイミング。ローカル時刻をミリ秒単位で書くのがよさそう
    - 入力イベントとしてInputEventをそのまま出力してもよい
        - 処理せずpassthroughしたイベントも記録する
    - モード切りかえ(tcodeモードからdirectモードなど)
    - 基本文字として入力された文字と2打鍵のキーコード(Translator.strToKeyの値)
    - キーシーケンスの途中(pending)の場合は、pendingになったことを記録
    - 部首変換が発生したときは、その事実と、入力した文字と置きかえた文字数
    - 交ぜ書き変換の場合は変換の開始/候補選択/確定などの操作および確定文字列
        - 候補選択を使用せず、単一候補をすぐに確定した場合はその事実
    - PendingKakuteiが作られたときのyomiString, kakuteiString
    - PendingKakuteiをキャンセルしたときはその事実
    - その他、アクションが発動した場合
- ログに記録しなくてよい情報
    - excludedAppsに該当したために処理しなかった入力イベント

# 機能2: 打鍵visualizer

ロガーの記録したデータを元に、どのキーがどのタイミングで打鍵されたかを視覚化する動画を生成する

## 要求仕様

- ロガーの記録したデータを元に、キー配置上でどのキーをどのタイミングで押し、何が入力されたかを動画にする
- 入力としてロガーファイル名、出力動画ファイル名のstemを与える
- 出力するログの範囲を開始時刻、終了時刻でオプション指定できる
- rubyスクリプトとする
- 動画の画面レイアウト案
    - 基本キーレイアウト
        - 画面上の配列をロウスタッガードにする必要はない。オーソリニアに並べればよい
    - 機能ボタン(BackSpace、Space、Enter、Clear)を配置
        - 機能ボタンには部首変換、交ぜ書き変換も含める
        - RemoveLastPendingActionはClearボタンとして表示する
    - 基本キーにはボタンラベルは不要
    - 右上に時刻を表示する

```
[部首] [交ぜ書き]    23:44:59

□□□□□ □□□□□ [BS]
□□□□□ □□□□□ [Clear]
□□□□□ □□□□□ [Enter]
□□□□□ □□□□□
     [Space]
```

- レイアウト上のキー/ボタンで、アクティブになったものを赤く表示する
    - 打鍵されたキーを赤く表示する
    - 基本キーの2打鍵目で文字が入力されたときは、キートップにその文字を表示する
    - 部首変換発生時は部首ボタンを赤くする
    - 交ぜ書き変換によって即確定したときは、交ぜ書きボタンを一瞬赤くする
    - 交ぜ書き変換によって候補選択が発生したときは、確定するまで交ぜ書きボタンを赤くしておく
    - 基本キーの2打鍵目がスペースキーで、第1打鍵の文字を入力した場合、スペースボタンのキートップにその文字を表示する

- 追加機能案
    - pendingがある場合、各キーごとに「もし次にこのキーが押されたとき文字が入るのであればその文字」をうすい色で表示
    - 最近入力された文字の表示エリアを最上段に表示
        - この表示をするかしないかをコマンドラインオプションで指定可能にする
        - 「最近入力した文字」には直近20文字分程度を表示
        - 部首・交ぜ書き変換、Backspaceなどによって編集された場合も、再現可能であれば反映する

## 実装方針案

- 動画形式はアニメーションGIFとする
    - 別案: 実装がより容易であれば、mp4やwebpを採用してもよい
- 30FPSでフレームを描画し、可変FPSの動画ファイル(GIF, Webp)として保存する
    - mp4の場合は固定FPSでよい
    - gif, webp, mp4を選択可能にする必要はない。実装するとしても、追加機能として後回しにする
- 時刻表示はロガーデータから得られたローカル時刻とする。秒未満は表示しなくてよい(動画にする時点で、フレームの時刻と表示しているイベントの時刻は正確に一致しなくなるため)
- キーを赤く表示する場合、赤色を保持する時間は2フレーム
